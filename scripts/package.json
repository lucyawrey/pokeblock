{
  "name": "scripts",
  "private": true,
  "scripts": {
    "generate": "bun run generate_pokemon_spawns.ts && bun run generate_pokemon_drops.ts && bun run generate_archaeology_loot.ts && bun run generate_fishing_loot.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "csv-parse": "^6.2.1"
  }
}
